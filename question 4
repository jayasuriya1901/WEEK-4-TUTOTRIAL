#include <iostream>
using namespace std;

int main() {
    float x = 2.5;
    int y = 3;
    auto result = x / y;
    cout << "Data type of result: " << typeid(result).name() << endl;
    return 0;
}
